import { useState } from 'react'; import { Mail,Lock,ArrowRight } from 'lucide-react'; import Logo from './Logo';
export default function LoginScreen({onLogin,onGoRegister}:{onLogin:()=>void;onGoRegister:()=>void}) { const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); return <main className="min-h-screen flex items-center justify-center p-6"><div className="w-full max-w-sm"><div className="text-center mb-8"><div className="flex justify-center mb-5"><Logo size="lg" showText={false}/></div><p className="text-red-light text-xs uppercase tracking-widest">Kişisel performans alanın</p><h1 className="text-4xl font-black text-white mt-2">LUMIERE <span className="text-red-light">COACHING</span></h1><p className="text-muted text-sm mt-2">Koçun, planların ve ilerlemen tek yerde.</p></div><form onSubmit={e=>{e.preventDefault();if(email&&password)onLogin()}} className="glass-panel p-6 space-y-5"><label className="block text-xs text-muted">E-POSTA<div className="relative mt-2"><Mail size={16} className="absolute left-4 top-3.5 text-muted"/><input required value={email} onChange={e=>setEmail(e.target.value)} className="w-full bg-white/5 border border-white/10 rounded-xl p-3 pl-11 text-white outline-none focus:border-red" placeholder="ornek@eposta.com"/></div></label><label className="block text-xs text-muted">ŞİFRE<div className="relative mt-2"><Lock size={16} className="absolute left-4 top-3.5 text-muted"/><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="w-full bg-white/5 border border-white/10 rounded-xl p-3 pl-11 text-white outline-none focus:border-red" placeholder="••••••••"/></div></label><button className="w-full gradient-red text-white font-bold rounded-xl py-3.5 flex justify-center gap-2">Giriş yap <ArrowRight size={17}/></button><button type="button" onClick={onGoRegister} className="w-full border border-white/10 text-white rounded-xl py-3.5">Yeni hesap oluştur</button></form></div></main>; }
