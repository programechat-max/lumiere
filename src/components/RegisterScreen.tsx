import { useState } from 'react'; import { User,Mail,Lock,ArrowRight } from 'lucide-react'; import Logo from './Logo';
export default function RegisterScreen({onRegister,onGoLogin}:{onRegister:()=>void;onGoLogin:()=>void}) { const [valid,setValid]=useState(false); return <main className="min-h-screen flex items-center justify-center p-6"><div className="w-full max-w-sm"><div className="text-center mb-8"><div className="flex justify-center mb-5"><Logo size="lg" showText={false}/></div><p className="text-red-light text-xs uppercase tracking-widest">Hesap oluştur</p><h1 className="text-3xl font-black text-white mt-2">Yeni başlangıç</h1></div><form onSubmit={e=>{e.preventDefault();if(valid)onRegister()}} className="glass-panel p-6 space-y-4"><input required onChange={()=>setValid(true)} className="w-full bg-white/5 border border-white/10 rounded-xl p-3.5 text-white" placeholder="Ad Soyad"/><input required type="email" className="w-full bg-white/5 border border-white/10 rounded-xl p-3.5 text-white" placeholder="E-posta"/><input required minLength={8} type="password" className="w-full bg-white/5 border border-white/10 rounded-xl p-3.5 text-white" placeholder="Şifre (en az 8 karakter)"/><button className="w-full gradient-red text-white font-bold rounded-xl py-3.5 flex justify-center gap-2">Devam et <ArrowRight size={17}/></button><button type="button" onClick={onGoLogin} className="w-full text-muted py-2">Zaten hesabın var? Giriş yap</button></form></div></main>; }
