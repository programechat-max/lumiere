import { TrendingDown,Flame,Activity } from 'lucide-react'; import type {WeightEntry,DailyRing,UserProfile} from '../types';
export default function DailyScreen({weightData,rings,profile}:{weightData:WeightEntry[];rings:DailyRing[];profile:UserProfile}){return <main className="min-h-screen pb-20 px-5"><header className="pt-6 pb-5"><p className="text-red-light text-xs uppercase tracking-widest">Günlük takip</p><h2 className="text-2xl font-black text-white">Bugünün özeti</h2></header><div className="grid grid-cols-3 gap-2 mb-4">{rings.map(r=><div className="glass-panel p-3 text-center" key={r.label}><b className="text-xl text-white">{r.current}</b><p className="text-[9px] text-muted mt-1">/ {r.target} {r.unit}</p><div className="h-1 bg-white/10 mt-2"><div className="h-full" style={{width:`${Math.min(r.current/r.target*100,100)}%`,background:r.color}}/></div></div>)}</div><section className="glass-panel p-5 mb-4"><p className="text-xs text-muted uppercase tracking-widest">Kilo takibi</p><div className="flex items-end gap-2 mt-2"><b className="text-4xl text-white">{profile.weight}</b><span className="text-muted">kg</span><span className="text-emerald text-xs"><TrendingDown size={12} className="inline"/> 2.7kg</span></div><div className="flex items-end gap-1 h-24 mt-4">{weightData.map((d,i)=><div key={d.date} className="flex-1 bg-red-light/70 rounded-t" style={{height:`${45+i*12}%`}}/>)}</div></section><section className="glass-panel p-5"><div className="flex items-center gap-2 mb-4"><Flame size={16} className="text-red-light"/><b className="text-white">Haftalık ilerleme</b></div><div className="space-y-4"><div className="flex justify-between text-sm"><span className="text-muted">Antrenman sayısı</span><b className="text-white">2 / 5</b></div><div className="flex justify-between text-sm"><span className="text-muted">Ortalama kalori</span><b className="text-white">1,984 kcal</b></div><div className="flex justify-between text-sm"><span className="text-muted">Protein ortalaması</span><b className="text-emerald">165g</b></div></div></section></main>}
