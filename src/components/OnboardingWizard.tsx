import { useState } from 'react'; import { ArrowRight,ArrowLeft,Check,Camera,Mic,Sparkles } from 'lucide-react'; import Logo from './Logo';
export default function OnboardingWizard({onComplete,onSkip}:{onComplete:()=>void;onSkip:()=>void}){const [step,setStep]=useState(0);const [goal,setGoal]=useState('');const steps=['Hoş geldin','Temel bilgiler','Hedeflerin','Kamera izni','Mikrofon izni','Özet'];const next=()=>step<5?setStep(step+1):onComplete();return <main className="min-h-screen flex flex-col p-6"><div className="flex justify-between items-center"><div className="flex gap-1">{steps.map((_,i)=><div key={i} className={`h-1 rounded-full ${i<=step?'bg-red-light w-6':'bg-white/10 w-4'}`}/>)}</div><button onClick={onSkip} className="text-muted text-xs">Atla</button></div><div className="flex-1 flex flex-col justify-center text-center"><Logo size="lg" showText={false}/><h2 className="text-2xl font-black text-white mt-6">{steps[step]}</h2><p className="text-muted text-sm leading-relaxed max-w-xs mx-auto mt-3">Hedeflerine uygun, sürdürülebilir bir fitness planı oluşturalım.</p>{step===2&&<div className="space-y-2 mt-6">{['Yağ Yakımı','Kas Kazanımı','Güç','Dayanıklılık'].map(g=><button onClick={()=>setGoal(g)} className={`w-full glass-panel p-4 text-left text-white ${goal===g?'border-red bg-red/10':''}`} key={g}>{goal===g?<Check size={16} className="inline mr-2 text-emerald"/>:null}{g}</button>)}</div>}{(step===3||step===4)&&<button className="glass-panel p-5 mt-6 mx-auto flex items-center gap-3 text-white"><{step===3?'Camera':'Mic'} size={20} className="text-red-light"/>İzin ver (opsiyonel)</button>}{step===5&&<div className="glass-panel p-5 mt-6"><Sparkles className="text-red-light mx-auto"/><p className="text-white text-sm mt-3">Profilin hazır. Kişisel programını oluşturalım.</p></div>}</div><button onClick={next} className="w-full gradient-red text-white font-bold py-3.5 rounded-xl">{step===5?'Programımı oluştur':'Devam et'} <ArrowRight size={16} className="inline"/></button></main>}
